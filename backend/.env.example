# ========================================
# CampusPlay Backend Environment Variables
# ========================================

# Server Configuration
# --------------------
PORT=5000
NODE_ENV=production

# Database Configuration
# ----------------------
# MongoDB Atlas connection string
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
# IMPORTANT: Whitelist 0.0.0.0/0 in MongoDB Atlas Network Access for cloud deployments
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/campusplay?retryWrites=true&w=majority

# JWT Secret
# ----------
# CRITICAL: Use a strong random string (minimum 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long_change_in_production

# CORS Configuration
# ------------------
# Frontend URL for CORS (no trailing slash)
# Development: http://localhost:3000
# Production: https://your-frontend-domain.vercel.app
CORS_ORIGIN=https://your-frontend-domain.com

# Admin Configuration
# -------------------
# Admin email (can bypass @vit.edu.in validation)
ADMIN_EMAIL=admin@vit.edu.in

# ========================================
# Deployment Notes:
# - Render: Set these in Environment tab
# - Railway: Set in Variables section
# - Heroku: Use heroku config:set KEY=VALUE
# ========================================
